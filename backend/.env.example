# Environment Configuration
NODE_ENV=development # or production

# Server Configuration
PORT=5000 # Default port for the backend server

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/refugee_techpreneurs # Local MongoDB URL
MONGODB_URI_PROD=your-mongodb-atlas-connection-string # Production MongoDB URL (Atlas)

# Authentication
JWT_SECRET=your-secret-key-here # Generate a strong random string for JWT signing

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000,https://your-production-domain.com

# Admin Configuration
ADMIN_CODE=your-admin-registration-code # Code required for admin registration

# OpenAI Configuration
OPENAI_API_KEY=sk-your_openai_api_key_here # Get from platform.openai.com

# Note: Never commit the actual .env file with real values
# Replace the placeholder values above with your actual configuration
# For the JWT_SECRET, generate a strong random string
# For OPENAI_API_KEY, sign up at platform.openai.com to get your API key
